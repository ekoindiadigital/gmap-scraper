{
  "name": "gmap-scraper",
  "version": "1.0.0",
  "description": "Scrape Google Maps using Puppeteer on Render",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "PUPPETEER_EXECUTABLE_PATH=$(node -e \"console.log(require('chrome-aws-lambda').executablePath)\") npm install puppeteer"
  },
  "dependencies": {
    "chrome-aws-lambda": "^10.1.0",
    "express": "^4.18.2",
    "puppeteer-core": "^21.3.8"
  }
}
